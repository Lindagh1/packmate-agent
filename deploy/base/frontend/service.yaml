apiVersion: v1
kind: Service
metadata:
  name: packmate-frontend
  labels:
    app.kubernetes.io/name: packmate-frontend
    app.kubernetes.io/component: frontend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: packmate-frontend
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
