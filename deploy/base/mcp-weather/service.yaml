apiVersion: v1
kind: Service
metadata:
  name: weather-mcp
  labels:
    app.kubernetes.io/name: weather-mcp
    app.kubernetes.io/component: mcp
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: weather-mcp
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
