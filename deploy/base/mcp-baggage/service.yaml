apiVersion: v1
kind: Service
metadata:
  name: baggage-policy-mcp
  labels:
    app.kubernetes.io/name: baggage-policy-mcp
    app.kubernetes.io/component: mcp
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: baggage-policy-mcp
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
